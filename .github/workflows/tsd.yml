name: tsd

on:
  push:
    branches:
      - main
      - develop
  pull_request:

jobs:
  tsd:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - name: Setup Node.js
      uses: actions/setup-node@v5
      with:
        node-version: lts/*
    - name: Install
      run: npm install
    - name: Check type definitions
      run: npx tsd
